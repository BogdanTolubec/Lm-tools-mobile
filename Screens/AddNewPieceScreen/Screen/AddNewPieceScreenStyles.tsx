import React from "react";
import { StyleSheet } from "react-native";

const add_new_piece_styles = StyleSheet.create({
    wrapper: {
        flex: 1,
        flexDirection: "column",
    }
})

export default add_new_piece_styles